from setuptools import setup, find_packages

setup(
    name="terramind-demo",
    version="0.1.0",
    description="TerraMind demo",
    author="Caroline Arnold",
    packages=find_packages(),
    python_requires=">=3.12",
    install_requires=[
    ],
)